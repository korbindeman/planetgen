# @planetgen/bake

Named slot for the planet-scale bake: cubesphere faces, edge and corner blends, `WorldPipeline` against a sibling `~/dev/terrain-diffusion` checkout.

Nothing here runs yet. Do not put terrain-diffusion into this tree. See `docs/terrain.md`.
