---
description: Work happens in a project — Thalos by default, Earth as the reference fixture.
alwaysApply: true
---

# Projects

Work in this repo happens in a **project**. Thalos is the default. Earth is
the **fixture**, not a tree.

- Thalos is the world being discovered. Development, `bun run preview`,
  `bun run sheet`, and `bun run export:td` run against it unless Earth is
  asked for. The project file holds the adopted **body** (and pipeline
  notes). It does not hold a seed or a gene bag.
- Earth is the present-day reference fixture. Use `--earth` or
  `--project=earth`, or `bun run check:earth`. No search, no variants. Its
  file authors body plus fixture knobs and the `'earth'` token so shaping
  can be tested on a known tectonic base.
- The app starts on a project page. Opening a project persists it
  (`planetgen.activeProject`); a restart resumes that project. Switching
  means going back to the project page — there is no in-workspace dropdown.
  **New project** names the world and picks **body** buckets (size, age,
  day, water). Those buckets are ranges, stored on the project until an
  **adopted body** is set. User projects live at
  `preview/<name>/project.json`. Thalos and Earth stay shipped modules.
- A **variant** is a saved snapshot: layout seed, shape seed, body, gene
  draws, body **pins**, **ranges**, and a **name**. Its id is UTC unix
  time. Every **Save** writes a new snapshot. The same name continues
  that card; a new name is another variant. The panel shows one card per
  name. The catalog merges by id on load and write. Delete marks a
  snapshot (`deleted`); the id stays. `drop` is the only way an id
  leaves the file. The checkout on the globe is always a card. The UI
  word is Save, never Update. Explore likes write **ranges** to the
  current snapshot as they go. Pins are body-only. Earth has no variants.
  How this works: [docs/studio.md](docs/studio.md).
- Artifacts belong to a variant: `preview/<project>/v/<id>/`, catalog at
  `preview/<project>/variants.json`. Earth and a project with no variant
  still use `preview/<project>/`. Listing without a variant id does not
  walk `v/`. Loading another project or variant must not show the last
  one's tiles.
- CLI with no seed uses the last saved variant if there is one, else a
  working planet from the adopted body. Pass `--variant=` to pick a node.
  Finish runs on whichever variant you name; there is no Adopt/Commit step.
- Do not treat the generator's Earth-calibrated DEFAULTS as a third project.
  Those are the unmarked baseline the body sits on.
